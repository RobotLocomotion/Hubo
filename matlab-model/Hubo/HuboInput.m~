classdef HuboInput < LCMCoordinateFrame
  properties
      
  end
  methods
    function publish(obj,t,x,channel)
      % short-cut java publish with a faster mex version
      AtlasCommandPublisher(obj.mex_ptr,channel,t,[0*x;0*x;x]);
    end
  end
end